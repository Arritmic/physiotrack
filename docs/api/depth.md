# Depth

Monocular dense depth estimation with Depth Anything V2 (ViT-S/B/L). Returns a
[`DepthResult`](results.md#depthresult) whose `.plot(colormap=...)` colorizes the map.
See the [Depth guide](../guides/depth.md).

::: physiotrack.Depth
