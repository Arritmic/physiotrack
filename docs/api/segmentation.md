# Segmentation

Dense masks: body-part parsing (Sapiens Goliath), person/head masks (YOLO-seg), and
19-class face parsing (SegFace). Results carry a `seg_map` and optional per-instance
masks. See the [Segmentation guide](../guides/segmentation.md).

::: physiotrack.Segmentation
