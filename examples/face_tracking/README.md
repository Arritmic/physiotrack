# Face tracking example

This example detects faces in each video frame with [`physiotrack.Face`](../../docs/guides/face.md)
and associates the boxes over time with a [`physiotrack.Tracker`](../../docs/guides/tracking.md).
The annotated video shows face boxes, temporary track IDs and trails, plus a corner
panel with the per-frame face count, active-track count, detector model, and tracker.

From the repository root:

```bash
python examples/face_tracking/track_faces.py
```

For a quick CUDA smoke run or a different video:

```bash
python examples/face_tracking/track_faces.py --device cuda --max-frames 120
python examples/face_tracking/track_faces.py --input path/to/video.mp4 --device cuda
```

Add `--show` only in a desktop session; all outputs are saved whether or not a GUI
window is shown. The default output is `examples/face_tracking/results/`:

```text
results/
├── students_face_tracking_tracked.mp4
├── students_face_tracking_frames.jsonl
├── students_face_tracking_tracks.csv
└── students_face_tracking_summary.json
```

The JSONL file has one self-contained JSON object per frame, including the face
detector `Result`, tracker `TrackResult`, counts, timestamp, and active IDs. The CSV
has one row per active track per frame, which is convenient for trajectory analysis.
The summary JSON records input metadata, configuration, aggregate timing and every
temporary ID observed. OpenCV writes the annotated video without copying source audio.

See the [documentation walkthrough](../../docs/guides/face-examples.md) for field
tables and interpretation guidance.

## Tracking is not recognition

An integer ID means that the tracker associated a sequence of boxes in this one
video. It is not a person's name or biometric identity, and it can change after an
occlusion or exit/re-entry. The bundled synthetic clip has no ground-truth tracks;
inspect it qualitatively, but do not report its IDs as an accuracy benchmark.

## Example Assets & Licensing

The video in [`data/`](data/) is a synthetic Google Gemini asset generated on
**August 13, 2026**. The contributor confirms that its people and scene imagery was
created using **Nano Banana 2** (**Gemini 3.1 Flash Image**). Nano Banana 2 is an
image-generation model; the separate model used to animate or encode the supplied
video was not recorded, so this example does not assign video generation to Nano
Banana 2 itself.

- **Purpose:** PhysioTrack includes the clip for open scientific research, software
  evaluation, testing, and documentation. It is not a real-person or biometric
  dataset.
- **Asset licence:** The contributor dedicates any copyright and related rights they
  hold in the media file under
  [CC0 1.0 Universal](https://creativecommons.org/publicdomain/zero/1.0/). CC0
  permits commercial as well as non-commercial reuse. “Research” describes the
  purpose of this example, not an additional use restriction. The example code and
  documentation remain under the repository's
  [GPL-3.0-or-later licence](../../LICENSE).
- **Google output terms:** Google's
  [Terms of Service](https://policies.google.com/terms) state that Google does not
  claim ownership of original content generated by applicable services. That
  statement is distinct from the contributor's CC0 dedication above.
- **Privacy and ethics:** Every face, personal trait, and individual feature in the
  clip is computer-generated and does not depict a real living person. Any
  resemblance to an actual person is coincidental.
- **Digital provenance:** The repository preserves the contributor-supplied MP4
  byte-for-byte, including its Google encoder metadata and any embedded **SynthID**,
  if present. SynthID is an invisible watermark rather than an ordinary metadata
  field; its presence in this clip has not been independently checked with Google's
  [Gemini verification tool](https://support.google.com/gemini/answer/16722517).

See [`data/MEDIA.yml`](data/MEDIA.yml) for the checksum and machine-readable
provenance.
